import React from 'react'
import PropTypes from 'prop-types'

Example.propTypes = {
	id: PropTypes.number,
}

export default function Example(props) {
	return (
		<div>
            Hello, world!
		</div>
	)
}

