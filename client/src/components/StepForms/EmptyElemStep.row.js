import React from 'react'
import PropTypes from 'prop-types'

EmptyElemStepRow.propTypes = {
	id: PropTypes.number,
}

export default function EmptyElemStepRow(props) {

	return (
		<div>
            Empty element step. Nothing happens over here
		</div>
	)
}

